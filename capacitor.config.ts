import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.umy.educator',
  appName: 'educator-app',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
